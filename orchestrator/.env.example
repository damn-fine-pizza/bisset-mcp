# Copy to .env and fill in your values
OPENAI_API_KEY=sk-...
# When false, agents will use local prompt templates instead of calling external LLMs
USE_LLM=false
# Optional: override the model (default: gpt-4o-mini)
# LLM_MODEL=gpt-4o
