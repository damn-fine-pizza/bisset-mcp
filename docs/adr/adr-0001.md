# ADR 0001: Deterministic workflow driver

Status: Proposed

## Context
We need a deterministic engine-driven workflow driver callable via MCP.

## Decision
Implement `workflow_tick` that returns the single next action the client must perform.

